# 雨夜便利店游玩版验收

2026-09-12，本机浏览器预览 `http://127.0.0.1:4180`。

## 自动回归

`npm test`：20 项通过，0 项失败。覆盖自动门、玻璃与货架碰撞、后门、摄像机裁切、8,000 次冲刺移动、移动后的自行车与箱子、库存、余额、小票和未付款限制。测试源文件为 `tests/gameplay.test.mjs`。

## 实际浏览器游玩

- 官方瓦雷莎模型及贴图加载成功；站立、移动、转身与交互姿态可见。角色来源保存在 `assets/character/SOURCE.md`。
- WASD 实际移动到自动门，门体滑开；角色从道路跨上人行道并走入店内。前门开口缩为 2.2 米，滑开的门片仍位于建筑门面内。
- 实际拿取海盐薯片，购物篮增加 1 件，商品从货架移除并被角色手持。
- 未付款时按 R 被正确拒绝；在收银台结账后余额从 ¥2,000 变为 ¥1,870；随后可食用，产生 1 件空包装。
- 实际制作咖啡：投币后余额为 ¥1,880，冲泡完成后出现“取走热咖啡”，取杯成功。
- 实际跑步离店，在雨伞架借伞并撑开。
- 实际骑行后停车：自行车停在新位置 x=5.05、z≈0.03，角色下车到 x=4.25、z≈0.03，未穿越旁边护栏或玻璃。停车后的对象仍为同一辆自行车。
- 实际切换 Tab：纯观赏模式的可见 DOM 仅剩场景画布，随后可返回游玩；角色和 HUD 在观赏模式隐藏。
- 近柜台时原先镜头太近，已经通过绕开遮挡物的候选视角修正；修正后的店内截图能显示完整角色。
- 浏览器没有运行错误；Three.js 0.170 的 MMDLoader 会输出一条版本弃用提示。依赖已固定，不影响本地运行。
- 测试时通常约 48–54 FPS，随窗口和视角变化；此数值只代表当前设备。

## 文件与边界

主产物为完整内联的单文件 HTML；无外部脚本、模型、贴图请求。浏览器工具禁止 `file://` 导航，因此工具内通过本机 HTTP 验证同一产物。

此版本是围绕固定街角构建的单人互动场景。物品与余额在当前会话内保留，刷新页面会重新开始。实际手机触控移动未验收，主要操作面向键盘和鼠标。

官方模型和原始贴图保持原始署名，行走和生活交互动作为本场景制作。
