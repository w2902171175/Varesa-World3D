# start.bat 启动与分享检查

- Windows 批处理从项目目录启动成功，Node.js 22.23.1。
- 现有 4180 预览保持运行；启动器识别已占用端口，自动选择 4181。
- 浏览器实际打开新服务提供的离线游戏，角色、界面及 Alt 提示正常，未记录 JS 错误。
- 两个启动器分别占用 4181、4182；按 Enter 停止其中一个不会影响另一个，也不会覆盖后启动实例的地址文件。
- 正常停止后端口释放，根目录 `游玩地址.txt` 更新为已停止。
- 服务器测试覆盖固定文件列表、GET/HEAD、健康检查、gzip/Brotli、缓存更新及禁止访问项目源码与压缩包。
- Cloudflare 2026.9.1 Windows 工具已从官方 GitHub 下载，SHA256 与官方 release digest 一致；版本命令及所用参数解析通过。
- 公网日志测试要求同时收到正规 trycloudflare 地址及已建立连接的日志才显示分享成功；处理跨数据块、启动取消与地址伪装。

初次交付仅验证本地服务。本轮根据真实失败日志修复了强制 HTTP/2 的问题，改用自动协议；网络检测中 QUIC 成功、HTTP/2 失败。

2026-09-12 的真实公网验证已完成：QUIC 隧道注册成功，公网健康检查返回测试实例标识，入口页面及完整游戏 HTML 返回成功，游戏全文 SHA256 与本地文件一致。测试隧道和独立服务器在验证后已停止。结果见 `public-tunnel-result.json`。

本机系统 DNS 对新域名存在解析失败，独立验证使用公共 DNS 的真实地址并保留正常 TLS 证书校验，没有修改系统或代理设置。启动器现在会提示本机 DNS 解析异常，避免与隧道连接失败混淆。源码中的工具下载入口及校验值均来自官方 release。

复查命令：`npm test`、`node qa/launcher-smoke.mjs`。后者只测试本地服务器，不会连接公网隧道。

`node qa/public-tunnel-smoke.mjs` 是另一个手动验证命令，会临时发布游戏、验证完整传输，然后关闭测试隧道。
