# 场景检查记录

日期：2026-09-12。

- 生产构建成功；全部源文件通过 JavaScript 语法检查。
- 本机浏览器实际运行 `Output/index.html`（本地预览地址 `127.0.0.1:4179`），主视角无控制台错误或警告。
- 实际拖动验证环绕旋转；滚轮验证缩放；键盘验证俯视、缩放；双击验证初始视角恢复。
- 高角度俯视屋顶逐渐淡出，饮料冷柜、货架、饭团便当、收银区和后场门均为三维物件，能从上方查看。
- 背巷视角检查电线杆、公告栏、空调与货箱接地，修正货箱穿墙。
- 画面 DOM 只有一个观看画布，无按钮、输入框或选择器。
- 依赖、着色器和程序材质均内联。HTML 不引用外部脚本或样式文件。
- 浏览器工具自身禁止 `file://` 导航，因此直接双击离线入口的浏览器启动方式未由该工具实测；通过本机 HTTP 预览验证了同一 HTML 的完整渲染和交互。
- 主视角实测约 72 FPS，此读数仅代表当前测试设备与窗口。
- 支持触控事件及窄屏构图；未使用真实手机实测。

输出主预览图在 `Output/预览.png`，辅助视角在此目录。
