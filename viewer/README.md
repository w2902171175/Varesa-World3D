# 雨夜街角观赏版

本目录保存不依赖瓦雷莎模型的微缩街角场景源码，源于项目早期的纯观赏版。它保留雨夜便利店、店内陈设、街边设施、反光路面和旋转/缩放操作，不包含人物、背包或购物玩法。

在项目根目录运行 `npm ci`，然后运行 `npm run package:viewer`。生成的 HTML、许可证和 ZIP 放在被 Git 忽略的 `release/` 中，供手动上传 GitHub Release。完整互动版仍按照项目根目录 README 获取模型后本地构建。
