# 雨夜街角观赏版

本目录保存不依赖瓦雷莎模型的微缩街角场景源码，源于项目早期的纯观赏版。它保留雨夜便利店、店内陈设、街边设施、反光路面和旋转/缩放操作，不包含人物、背包或购物玩法。

仓库根目录已提供可直接下载的 `Varesa-World3D.html`。需要重新生成时，在项目根目录运行 `npm ci`，然后运行 `npm run build:viewer`；构建脚本会更新同名文件。项目与 Three.js 的 MIT 许可证文本已写入该 HTML。完整互动版仍按照项目根目录 README 获取模型后本地构建。
